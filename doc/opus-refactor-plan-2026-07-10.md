Ты уже проанализировал код. Теперь нужен КОНКРЕТНЫЙ план рефакторинга.
Шагами. Без общих слов.

## Что известно

- ScriptRunnerService + ScriptEngine + UploadProgress + DEFAULT_SCRIPT — мёртвый код, нигде не запускается
- MainActivity ~750 строк — всё делает сама (BT, HTTP, БД, диагностика, чат, история)
- Хост "https://obdai.ru" захардкожен в MainActivity ~5 раз
- checkServer/checkLlm/чат используют HttpURLConnection без X-Api-Key (рядом есть ServerClient на OkHttp с ключом)
- ElmChecker.getElm() течёт инкапсуляцию — ElmProtocol дёргается в обход
- DEFAULT_SCRIPT не используется в живом коде (MainActivity передаёт "" как fallback)
- ui/parts/ — пустая папка

## Что нужно от тебя

План рефакторинга в виде пронумерованных шагов. На каждый шаг:

1. Какие файлы ТРОНУТЬ (удалить, создать, изменить)
2. Что КОНКРЕТНО сделать (какие методы перенести, какие строки заменить)
3. Результат шага (что изменилось)

## Ограничения

- Не вводить Hilt/Koin/Room/Coroutines — без фреймворков
- Не менять UI и сигнатуры API-эндпоинтов
- Сохранить foreground-сервис для долгой диагностики (оживить ScriptRunnerService или создать заново — на твой выбор)
- Сохранить динамический тест (DynamicCollector)
- Сохранить чат с LLM

## Формат ответа

Только нумерованный список шагов. Без введения, без резюме.
